"""Connecteur Nakala (Huma-Num) — lecture (P1).

`client` : client HTTP lecture seule (httpx). `mapper` : conversion d'un
dépôt Nakala (JSON) vers une structure neutre alignée sur le modèle
ColleC. Aucune écriture en base ici (c'est le rôle des services P1b+).
"""
